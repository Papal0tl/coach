# Skill Matrix

Scale:

- `0`: not observed.
- `1`: needs heavy prompting.
- `2`: succeeds with light prompting.
- `3`: independent and reliable.

| Skill | Score | Evidence | Last Updated |
| --- | ---: | --- | --- |
| Problem restatement | 0 | Not observed. | — |
| Constraint analysis | 0 | Not observed. | — |
| Brute-force construction | 2 | Correctly described O(n²)/O(n³) brute force in both session blogs without prompting. | 2026-06-03 |
| Pattern recognition | 3 | First-attempt solves for sliding window (LC 76) and DP (LC 53). Independently reached shrinking-boundary simulation (LC 54), 4-way cycle rotation (LC 48), staircase search (LC 240), two-pointer list redirect (LC 160), and iterative in-place reversal (LC 206) without scaffolding. LC 234 correctly identified the no-backward-traversal constraint and chose the value-copy workaround unprompted, though the O(1)-space fast/slow-pointer composition was declined. LC 141 closed that gap: hash-set solution first (zero bugs), then named and wrote correct fast/slow pointer code unprompted in the blog. LC 142 correctly scoped the problem as needing the entry node (not just detection), but reverted to a full decline of the Floyd's-algorithm extension — the hands-on-implementation gap for this specific technique remains open after 2 sessions where it was offered. LC 21 (merge-two-sorted-lists) required two rounds of hints to complete the first draft (uninitialized pointer, naming mismatch), but the dummy-node technique itself was named and applied independently once the underlying question ("where to attach the first node") was raised. LC 2 (add-two-numbers) transferred the dummy-node technique with zero prompting this time, and independently recognized the reverse-digit-order-matches-addition-order insight; the digit-wise carry-propagation mechanism itself needed two guided-trace hints (missing carry variable, missing trailing-carry loop condition) before being fully correct. LC 19 (remove-nth-node-from-end-of-list) transferred the dummy-node technique a third consecutive time with zero prompting, and independently framed the key insight as converting a from-the-end position to a from-the-front position via a length-counting pass; the two-pass structure was correct in the first draft. LC 24 (swap-nodes-in-pairs) transferred the dummy-node technique a sixth consecutive time with zero prompting, and correctly implemented a three-pointer pairwise rewiring (`a`, `b`, `prev`) on the first attempt, zero bugs. LC 25 (reverse-nodes-in-k-group) independently generalized the LC 24 pairwise pattern to arbitrary group size k, correctly framing the key insight as needing to check k nodes exist before reversing anything — a new form of lookahead-then-commit reasoning not previously tested in this arc. LC 138 (copy-list-with-random-pointer) independently named "hash map, old node to new node" as the fix for a forward-referencing pointer, after one guiding question and before any code was written — first session combining a hash map with linked-list construction rather than pure pointer rewiring. LC 148 (sort-list) first attempt was a bubble-sort-style adjacent swap, not merge sort — the only session so far where the first strategic instinct for a new pattern was actually wrong rather than merely incomplete — but the user abandoned it unprompted after empirically discovering (by running the code) that it broke list reachability, then independently composed the fast/slow midpoint technique with the merge-two-sorted-lists merge routine into a correct divide-and-conquer solution, and found a genuinely valid alternative midpoint-finding variant (tracking `prev` inside the loop) distinct from the hinted fix. LC 23 (merge-k-sorted-lists) transferred the merge-two-sorted-lists routine unprompted, folding it sequentially across all k lists — valid and correct, but not the divide-and-conquer decomposition of the list-of-lists that sort-list's own follow-up notes predicted as the next transfer test; that comparison was reached only through guided complexity reasoning after the fact, not as the initial strategic choice. LC 146 (lru-cache) is the first design/class-based session in the arc and the first hash-map-of-mutable-linked-list-nodes composition: the first draft independently defined a `Node` class and a `self.cache` dict with zero hints, correctly scoping the problem as a stateful class design and reaching the full hash-map + doubly-linked-list shape cold — no scaffolding phase at all, on a genuinely new problem *type* (not just a new instance of a known family). LC 94 (binary-tree-inorder-traversal) is the first tree-traversal session in the arc: went directly from an empty stub to a fully correct recursive left/visit/right DFS with the right base case, zero bugs, all 7 reference tests passed on the first run — the cleanest possible signal on a genuinely new structural domain (tree, not array/matrix/linked-list). LC 104 (maximum-depth-of-binary-tree) transferred the recursive-DFS shape to a *combine* traversal (return and merge child results) rather than a *visit* traversal (collect values in order) — first draft had the correct `1 + max(maxDepth(left), maxDepth(right))` structure with zero hints, confirming the recursion pattern generalizes beyond the specific inorder-visit shape tested in LC 94. LC 226 (invert-binary-tree) transferred the recursive-DFS shape a third way, to a *mutate-and-return* traversal (swap children, recurse to mutate subtrees in place, return the same node) — correct on the first commit with zero hints, and when asked why the recursive calls' return values could be safely discarded, correctly reasoned through in-place mutation/aliasing rather than treating the code as a memorized template. Three tree sessions now cover three distinct recursive shapes (visit, reduce-to-scalar, mutate-in-place), all transferred cleanly. LC 101 (symmetric-tree) tested a fourth recursive shape — pair comparison (recursing on two nodes at once to check a mirror relationship) — and this is the first tree session where the core recursive shape did not transfer on the first draft: three drafts kept trying to force the single-argument `isSymmetric(root)` to do the comparison directly, and it took two guided questions before a two-argument helper (`compare(left, right)`) was introduced. Once introduced, the crossed-child recursion (`left.left` vs. `right.right`, `left.right` vs. `right.left`) was written correctly and unprompted on the very next draft — the scaffolding gap was specifically about recognizing the need for a paired-argument recursion, not about the crossed-comparison logic itself. | 2026-07-17 |
| Invariant formulation | 3 | Stated index-as-hash-map invariant precisely and unprompted (LC 41). Correctness argument in LC 73 blog stated iff condition correctly on first draft. LC 160 and LC 206 correctness arguments were both accurate, precise, and complete on first submission with zero revisions; LC 206 additionally stated the loop invariant verbally, unprompted, when asked directly. LC 234's correctness argument needed one revision (initially left blank) but was accurate once written. LC 141's blog correctness argument cleanly separated the no-cycle case from the cycle case (shrinking-gap reasoning), accurate on first submission. LC 142's correctness argument (hash-set entry-node reasoning) was accurate on first submission. LC 2's correctness argument precisely explained what `carry` represents at loop entry and why the loop condition must also check `carry != 0`, accurate on first submission. LC 24's correctness argument stated the pairwise-rewiring invariant precisely and unprompted, accurate on first submission; the user also correctly reasoned live, when asked, about why reassigning `prev.next` doesn't affect what `b.next` reads (distinct object references). LC 25's correctness argument stated the `group_prev` invariant precisely and unprompted ("points to the node right before the next group that has not been processed yet; all groups before it have already been reversed and reconnected"), accurate on first submission. LC 138's correctness argument was the most complete yet: precisely stated the post-first-pass guarantee (`mapp` holds a copy for every original node) and explicitly argued the second pass resolves forward, backward, self-referential, and `None` `random` pointers alike — accurate on first submission. LC 148's correctness argument was blank on first submission (a recurrence of the "blank on first draft" pattern seen earlier in the arc, e.g. LC 234), but once revised was accurate and complete: correctly stated the base-case guarantee, the recursive sorted-halves invariant, and the merge-step comparison/tail-attach argument. LC 146's correctness argument stated the recency-order invariant precisely and accurately on first submission: the list is always kept MRU-to-LRU, every touch moves a node to the front, so `tail.prev` is always the correct eviction target — no revision needed. LC 94's correctness argument accurately explained why left-visit-right recursion, applied uniformly at every level, produces inorder order for the whole tree (not just the top level), accurate on first submission. LC 104's blog correctness argument (larger child depth plus one for the current node) was accurate but did not explicitly name the base case (`None -> 0`) as the induction anchor — flagged as an optional, non-blocking gap. Notably, when asked directly in conversation to state the invariant (separately from the blog), the question was declined in favor of moving to the blog — the first session where the invariant question itself, not just an optional code follow-up, was skipped. LC 226's blog correctness argument used explicit induction on subtree size (base case, inductive step, conclusion) — the most formally structured correctness argument in the arc so far. When asked in conversation why the recursive calls' return values could be discarded, answered immediately and correctly: the recursion mutates the existing subtree in place rather than building and returning a new one — this time the invariant-adjacent question was answered directly, breaking LC 104's just-prior decline. LC 101 (symmetric-tree)'s blog correctness argument stated the mirror invariant precisely and completely: base cases for both-None/one-None/values-differ, then the crossed-recursion case, concluding "returns True if and only if the entire tree is symmetric" — accurate on first submission, no revision needed. LC 543 (diameter-of-binary-tree) is the first session where invariant formulation itself was the core blocking bug rather than a write-up gap: conflating the returned height with the tracked diameter (and failing to persist the tracked value via a shadowed local) took two guided-question rounds to resolve, the most scaffolding this skill has needed since first-missing-positive. Once resolved, the blog's correctness argument and key insight both stated the height-vs-diameter distinction precisely, and the final code's invariant (`self.length` holds the best diameter seen at any node visited so far; `getDepth` returns height to the caller) was correct and consistent with the fix. | 2026-07-17 |
| Data structure selection | 3 | Reached for two frequency maps + coverage counter without hints (LC 76). LC 138 reached for a hash map keyed by node identity to solve a forward-reference problem, unprompted. | 2026-07-09 |
| Complexity analysis | 2 | Stated O(n) time and O(n) space correctly for rotate-array without prompting. Consistent across sessions, including LC 234 (O(n)/O(n) for the brute force, stated unprompted) and LC 141 (O(n)/O(n) for the hash-set attempt, then O(n)/O(1) for the fast/slow alternative, both unprompted). LC 142 stated O(n)/O(n) correctly for the submitted hash-set solution; the O(1)-space target complexity was not reached this session. LC 21 stated O(n+m)/O(1) correctly in the blog, but only after being asked twice in conversation and moving to the blog instead of answering directly — written complexity is reliable, verbal on-demand statement was skipped this session. LC 24 stated O(n)/O(1) correctly in the blog with zero prompting. LC 138's blog (agent-prefilled Complexity section, confirmed by the user) stated O(n)/O(n) correctly; not new independent evidence this session. LC 23 named O(N·k) vs. O(N log k) correctly on the first ask, but the first explanation of *why* was vague and didn't isolate the log k factor; fully self-derived once broken into a concrete guided trace (count rounds and per-round work for k=8) — complexity comparison between two *different* algorithms for the same problem, not just analysis of a single chosen approach, is a new evidence type for this row. LC 94's Complexity section (agent-prefilled O(n) time / O(h) space, unchallenged) was accepted as-is in the blog; not new independent evidence this session. LC 226 (invert-binary-tree) stated O(n) time / O(h) space correctly and unprompted when asked directly in conversation, and named the recursion stack as the space driver — genuine on-demand recall, not just acceptance of a prefilled blog section. | 2026-07-15 |
| Edge-case design | 2 | Handled all-negative (Kadane) and duplicates-in-t (sliding window) correctly in code; edge cases in blogs are accurate. LC 141 blog correctly covered empty list, single-node no-cycle, and single-node self-loop. LC 142 blog edge-case checklist was complete and accurate (empty list, no-cycle single node, self-loop, cycle at head, cycle at tail). LC 21 blog edge-case checklist was complete and accurate (both empty, one empty, duplicates, non-interleaved ranges, single node), matching the reference test suite. LC 2's edge-case list (agent-prefilled, not independently re-derived) matched the reference tests; not new independent evidence this session. LC 24's edge-case list (agent-prefilled: empty, single node, odd-length, two-node) matched the reference tests; not new independent evidence this session. LC 138's edge-case list (agent-prefilled: empty list, self-referential random, no-random-pointers, duplicate values, original-list-unmodified) matched the reference tests; the empty-list guard was independently added in code before being asked. LC 94's first attempt (recursive DFS with `if node is None: return`) correctly handled the empty-tree, single-node, and skewed-tree cases without any special-casing in code — the base case alone was sufficient for every edge case in the reference suite. | 2026-07-14 |
| Debugging discipline | 2 | No bugs in LC 53, LC 48, LC 160, LC 206, LC 141, LC 24, or LC 94 (seven zero-bug first attempts on the code actually run). LC 234 broke the streak with three syntax-level bugs (4-arg `range()`, wrong index source, lowercase `true`/`false`) — none self-caught before running, but each resolved after one guided question/trace. In LC 76, found range bug after prompt to trace; fixed structural issue after direct callout. LC 160's only gap (`!=` vs `is not`) was self-caught while writing the blog, before being asked; LC 141 repeated the identical `==` vs `is` pattern in the blog's fast/slow code, this time not self-caught (flagged in review, not blocking since default `__eq__` made it safe). LC 142 had one mechanical `null`→`None` slip (JS/Java carryover), fixed after one guided question; the blog's first draft then misattributed the fix to an unrelated loop-condition change, corrected in one revision. LC 21 had zero logical bugs; two mechanical bugs (an uninitialized/misnamed tracking pointer, a `Listnode`/`ListNode` case typo, and returning the sentinel instead of `.next`) were all resolved after guided questions, none self-caught before running. LC 2 had two logical bugs this time (not mechanical): missing carry variable entirely, and missing trailing-carry loop condition — both resolved after guided-trace questions, neither self-caught before running. LC 19 had one omission bug (`dummy = ListNode(0)` never linked to `head`, so `dummy.next` stayed `None` and crashed with `AttributeError`), not self-caught before running but fixed immediately (correct diagnosis in one reply) once asked to trace what `dummy.next` pointed to. LC 24 had zero bugs: first attempt passed all 6 tests immediately. LC 25 had zero logical bugs; two mechanical bugs (`while true` instead of `while True`, and `kth` never advancing inside the k-step lookahead loop) were each caught via a single targeted trace question (asked to trace the loop by hand, then asked to run the code and read the resulting error) and self-fixed correctly on the first try. LC 138 had zero logic bugs, but four mechanical ones: the entire method body was first written directly in the class body instead of inside a `def`, causing a repeated `SyntaxError: 'return' outside function` (the biggest blocker of the session, needing one direct explanation after two trace-style questions went unanswered); `Node(cur)` instead of `Node(cur.val)`; a near-miss infinite loop from a missing `cur = cur.next` in the second pass, self-fixed before ever running in that state; and a briefly-deleted `Node` class, restored in the next commit. Notably, the user did not meaningfully engage with mental-trace prompts this session (asked twice) but converged fast once told to actually run the file and read the real error — suggests an empirical-debugging preference over mental tracing for this user, worth leaning into directly in future syntax-heavy sessions. LC 148 had one incorrect first strategy (bubble-sort-style adjacent swap, which broke list reachability — discovered by running the code, not by mental trace) followed by a real multi-bug rebuild: missing `return` statement, a broken swap missing `prev`-pointer tracking, two separate `NameError`s (bare `sortList(...)` call instead of `self.sortList(...)`; `dummy` referenced before being defined), a wrong-variable bug (`right = fast` instead of `slow`, then `self.sortList(fast)` instead of `self.sortList(right)`, fixed in two separate rounds), and a missing merge-loop tail-attach that silently dropped almost every node. Every one of these was diagnosed and fixed by the user after being shown the actual running output or traceback, continuing the empirical-debugging preference noted in LC 138 — no bug was self-caught before running, but every bug converged in one round once the real behavior was shown. LC 23 had one real bug: copying `.val` into a pre-existing dummy node instead of relinking the actual node (`cur.next = list1`), which left `cur.next` permanently `None` and crashed with `AttributeError` on the second inner-loop iteration — not self-caught before the fix, but resolved correctly in one pass after being asked to run the code and trace what `cur` was bound to at the crash point, no further hint needed. LC 146 had zero logic bugs; the only bug was a mechanical `self.` scoping slip (bare `cache` instead of `self.cache`), present in `get` in the first draft (moot at the time since the draft had a larger structural gap and a syntax error) and recurring in both `get` and `put` in the second draft after the structural gap was fixed. Not self-caught before running either time; each time resolved in one pass after actually running the code and reading the resulting `NameError`, continuing the empirical-debugging preference established in LC 138/148. Third draft passed all 6 reference tests with zero further bugs. LC 94 (binary-tree-inorder-traversal) had zero bugs of any kind — the first attempt (recursive DFS) passed all 7 reference tests immediately, no run-fix cycle needed at all. LC 104 (maximum-depth-of-binary-tree) had two mechanical bugs: `maxDepth(root.left)` called as a free function instead of `self.maxDepth(root.left)` (`NameError`), and a bare `return` in the base case returning `None` instead of `0` (`TypeError` from `max(None, None)`); both fixed after one guided question each, neither self-caught before running. New regression this session: when first asked what a single-node-tree run would print, answered "return 1" without actually running the code — the real output was the `NameError` above. This is a different failure than the established "doesn't self-catch before running" pattern; it's a *predicted* answer presented as if observed, and is the first instance of this specific shape. Self-corrected in one round once explicitly told to run the exact command and compare. LC 226 (invert-binary-tree) had zero bugs of any kind — first attempt (swap-then-recurse, discarding return values) passed all reference tests immediately, no run-fix cycle needed. LC 101 (symmetric-tree) had zero logic bugs but six mechanical/structural ones across five drafts: comparing `root.left == root.right` by object identity instead of mirroring; dead recursive calls placed after an early `return`; single-node `self.isSymmetric(root.left)`-style calls instead of a two-argument helper; `NameError` from using `left`/`right` without assigning them from `root`; `SyntaxError: 'return' outside function` from a misindented final `return`; and a missing `root is None` guard causing `AttributeError` on the empty-tree case. Every one was diagnosed and fixed only after the agent actually ran the code and showed the real error, continuing the established empirical-debugging preference — no bug was self-caught or fixed from a mental trace alone. LC 543 (diameter-of-binary-tree) had a mixed profile: a `SyntaxError` typo and a stray-`self`-param `TypeError` were both mechanical, resolved in one round each via running the code and reading the traceback, continuing the empirical-debugging pattern; a `root`-vs-`node` variable mix-up was self-caught before ever running. The session's real blocker was the silent wrong-answer bug (shadowed local variable), which is a genuine logic/scoping bug, not mechanical — it needed two rounds of conceptual questioning (not trace-and-run) since there was no error to observe, only a wrong numeric output. This is a new debugging-discipline data point: the established "run it and read the error" strength doesn't directly apply when the bug produces a plausible-looking wrong answer rather than a crash; the user still resolved it correctly, but via reasoning about assignment sites rather than empirical output-reading. | 2026-07-17 |
| Code clarity | 2 | Clean solutions; paste artifact caused syntax error in LC 76. No issues in LC 53. | 2026-06-03 |
| Test design | 0 | Not observed (agent wrote tests). | — |
| Communication | 2 | Terse but accurate. Blog pattern-recognition section revised from listing problem names to describing signals (one revision required across all three sessions so far). LC 160, LC 206, and LC 141 blogs all needed zero revisions on substantive sections; LC 234 needed one revision cycle (blank correctness argument, inaccurate "N/A" mistakes section), resolved cleanly on first request. LC 142 needed one revision (a mistake bullet misattributed why the fix worked); corrected precisely on first request with no further back-and-forth. LC 21 broke this pattern: a requested revision (a misattributed "mistake" bullet, plus two missing real bugs) was explicitly declined rather than applied — first time a requested correction was not made. LC 2 repeated this exact pattern: Mistakes Made section fabricated two bugs that never happened and omitted the real first bug; revision was requested and declined again — second consecutive session with a declined revision, both on inaccurate mistake attribution. LC 19 ended the decline streak: two revisions were requested (Mistakes Made described reasoning instead of the real bug; Brute Force duplicated the Final Algorithm) and both were fully and accurately applied on the first pass. LC 24 needed zero revisions — blog was accurate and complete on first submission, including the first-ever clean (non-duplicate) Brute Force section without a revision request. LC 25 needed zero revisions — Mistakes Made section was fully accurate against actual git history with no fabricated or misattributed bugs, the first clean instance of this specific section in the linked-list arc. LC 138 needed one revision: Mistakes Made initially omitted the two most significant bugs (the method-wrapping syntax error, the near-miss infinite loop), keeping only two minor ones — the underlying omission tendency recurred on a genuine multi-bug session, but the revision was requested and fully, accurately applied on the first pass, continuing the acceptance trend since LC 19 rather than the earlier decline pattern (LC 21, LC 2). Two messages this session were sent in Chinese despite the English-only preference — first observed lapse of that specific norm. LC 148 needed two revisions: a blank Correctness Argument (revised accurately and completely on request) and an inaccurate Mistakes Made section (two fabricated bugs, three real ones omitted) — this revision was explicitly declined, the first mistakes-section decline since LC 21/LC 2, though the correctness-argument-type revision was accepted cleanly, suggesting the acceptance/decline split may now depend on *which* section is flagged rather than being a uniform disposition. LC 23 needed one revision (Mistakes Made fabricated a "`return merge` inside the for loop" bug never present in any committed version, mischaracterized a real clarity edit as a bug fix, and omitted the two real bugs) — declined for a second consecutive session, extending rather than breaking the sort-list pattern; all other sections (Correctness Argument, Key Insight, Complexity) were accurate and needed no revision. LC 146 needed zero required revisions — Correctness Argument, Key Insight, Brute Force, and How I Will Recognize This Pattern Next Time were all precise and accurate on first submission. Mistakes Made described genuine conceptual clarifications rather than the session's two actual bugs (the repeated `self.cache` `NameError`, the incomplete first-draft `__init__`) — a milder variant of the recurring Mistakes-section gap, substitution rather than fabrication or omission. This was flagged as optional (not required, since the substituted content was itself accurate) rather than a required correction, and the user declined the addition — the first test of this gap framed as optional rather than as a factual-accuracy fix. LC 94 needed zero required revisions — Key Insight, Correctness Argument, and How to Recognize sections were all precise on first submission, and Mistakes Made accurately reported "N/A" for a session independently verified to have had zero bugs — the first fully clean *and* independently-confirmed-true Mistakes Made section in the arc (distinct from LC 24/LC 25, where the section described real friction/reasoning rather than literally reporting "no bugs"). LC 104 needed one required revision: Mistakes Made initially said "N/A" despite two real bugs in the committed history — unlike LC 94, this "N/A" was independently checked and found false. Revision was requested and applied fully and accurately on the first pass (both real bugs correctly named), continuing the acceptance trend since remove-nth-node-from-end-of-list/copy-list-with-random-pointer rather than the earlier decline pattern (sort-list, merge-k-sorted-lists). The optional add-on (noting the unverified "return 1" prediction) was requested but not included — declined only the optional part, not the required correction. LC 226 (invert-binary-tree) initially submitted an accurate "N/A" Mistakes Made (correctly, since the session had zero bugs), then in a later revision replaced it with two fabricated bugs that closely mirrored LC 104's actual bugs from the immediately preceding session — a new sub-variant of the recurring pattern (borrowing a different session's real content rather than inventing something wholly novel). Flagged against the commit history (single clean commit); the user reverted to "N/A" immediately with no pushback or justification attempted — the fastest, most frictionless correction of any Mistakes-Made inaccuracy observed in the arc. LC 101 (symmetric-tree) needed one required revision: Mistakes Made initially listed four of six real bugs, omitting the `NameError` and `SyntaxError`. Before being asked, the user had already added the `NameError` bullet independently; once the agent flagged the remaining `SyntaxError` omission, it was added immediately with no pushback — the fastest, most cooperative resolution of the omission variant of this pattern yet, combining self-correction with instant acceptance. Correctness Argument, Key Insight, and How to Recognize sections were all accurate on first submission. LC 543 (diameter-of-binary-tree) needed one requested revision: Mistakes Made accurately covered the core conceptual bug with zero fabrication, but omitted three real mechanical bugs (a `SyntaxError` typo, a stray-`self`-param `TypeError`, a self-caught `root`/`node` mix-up). Unlike symmetric-tree's immediate cooperative fix, this revision was explicitly declined ("no need to revise") — the first declined revision since the merge-k-sorted-lists/sort-list stretch (2026-07-10/11), breaking a 3-session streak of accepted revisions (remove-nth-node-from-end-of-list, copy-list-with-random-pointer, maximum-depth-of-binary-tree). | 2026-07-17 |
